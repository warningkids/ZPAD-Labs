Лабораторна робота №6

Обробка відео з камери за допомогою OpenCV (C++)

📌 Опис роботи

Дана програма реалізує зчитування відео з веб-камери з подальшою обробкою кадрів у реальному часі.

Функціональні можливості програми:

- Зчитування відео з камери

- Відображення відеопотоку у вікні

- Реакція на натискання клавіш клавіатури

- Реакція на події миші

- Перемикання режимів обробки зображення

- Відображення лічильника кадрів та FPS (кількість кадрів за секунду)


Проєкт реалізовано з використанням модульної архітектури, що забезпечує чітке розділення відповідальності між компонентами.

🏗 Архітектура проєкту

Проєкт складається з таких модулів:

CameraProvider — отримання кадрів з веб-камери

KeyProcessor — обробка натискань клавіш та керування режимами

FrameProcessor — обробка зображення відповідно до обраного режиму

Display — відображення результату у вікні

Кожен модуль реалізований в окремих файлах .hpp та .cpp.

💻 Вимоги до системи
Операційна система:

Linux (перевірено на Ubuntu та Kali Linux)

Необхідне програмне забезпечення:

OpenCV

CMake (версія 3.10 або новіша)

g++

make

Апаратні вимоги:

Наявність веб-камери

Процесор з підтримкою стандарту C++17

Мінімум 2 ГБ оперативної пам’яті

⚙ Встановлення залежностей

Після клонування репозиторію виконайте команду:

./preinstall.sh

Скрипт автоматично встановить необхідні бібліотеки та інструменти для збірки проєкту.

🔨 Збірка (білд) проєкту

Для збірки виконайте:

./build.sh

Після виконання:

буде створено директорію build/

виконано конфігурацію через CMake

скомпільовано програму

▶ Запуск програми

Для запуску виконайте:

./run.sh

Після запуску відкриється вікно з відео з камери та активною обробкою зображення.

⌨ Керування програмою
Перемикання режимів обробки:

i — режим інверсії кольорів (Invert)

b — розмиття зображення (Blur)

c — детекція контурів методом Canny

n — звичайний режим без обробки (Normal)

Завершення роботи:

q — вихід з програми

ESC — вихід з програми

📊 Додаткові можливості

Відображення FPS (Frames Per Second) у реальному часі

Відображення лічильника кадрів

Обробка подій миші (за наявності реалізації)
