# Лабораторна робота №4  
## Візуалізація даних 2

---

## 📌 Опис роботи

Програма дозволяє будувати графік гармонічного сигналу з накладеним шумом та виконувати його фільтрацію в реальному часі.

Користувач може змінювати параметри сигналу та шуму за допомогою інтерактивного інтерфейсу.

---

## ⚙️ Функціональні можливості

- Побудова гармоніки:  
  **y(t) = A · sin(ωt + φ)**

- Накладання шуму (гауссівського)

- Інтерактивне керування:
  - амплітудою
  - частотою
  - фазою
  - параметрами шуму

- Вмикання / вимикання шуму (checkbox)

- Фільтрація сигналу (Butterworth filter)

- Відображення:
  - початкового сигналу
  - відфільтрованого сигналу

- Кнопка **Reset** для скидання параметрів

---

## 🏗 Архітектура

Основні компоненти:

- `harmonic_with_noise()` — генерація сигналу
- `apply_filter()` — фільтрація сигналу
- Інтерактивний UI:
  - Slider
  - CheckBox
  - Button

---

## 💻 Вимоги до системи

- Python 3.8+
- OS: Windows / Linux / macOS

---

## 📦 Встановлення залежностей

```bash
pip install -r requirements.txt
```
## **▶️ Запуск програми**

```bash
python main.py
```
## **🎮 Керування**

**Слайдери:**
Амплітуда — змінює висоту хвилі

Частота — змінює кількість коливань

Фаза — зсув сигналу

Сер. шуму — середнє значення шуму

Дисперсія — інтенсивність шуму

Зріз фільтра — сила фільтрації

**Чекбокс:**

Показувати шум — вкл/викл шум

**Кнопка:**

Скинути — повернення до початкових значень
